CREATE TABLE IF NOT EXISTS recorded_files (
    order_id integer NOT NULL,
    tuner_id integer NOT NULL,
    channel_id varchar(255) NOT NULL,
    program_name varchar(255) NOT NULL,
    record_size integer NOT NULL,
    start bigint NOT NULL,
    end bigint NOT NULL,
    CONSTRAINT record_files_pk PRIMARY KEY (order_id),
    CONSTRAINT recorded_tuners FOREIGN KEY (tuner_id)
    REFERENCES tuners (id)
    CONSTRAINT record_information FOREIGN KEY (order_id)
    REFERENCES tuners (order_id)
);